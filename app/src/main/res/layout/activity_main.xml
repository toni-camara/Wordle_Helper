<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background"
    android:orientation="vertical"
    tools:context=".MainActivity">

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/mainHeader"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="50dp"
        android:fontFamily="sans-serif-light"
        android:text="@string/app_name"
        android:textColor="@color/whiteText"
        android:textSize="30sp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_marginHorizontal="70dp"
        android:gravity="center_horizontal"
        android:orientation="vertical">

        <Button
            android:id="@+id/playBtn"
            style="@style/WhiteButton"
            android:text="@string/play_button"
            app:icon="@drawable/play_icon" />

        <Button
            android:id="@+id/searchBtn"
            style="@style/BlueButton"
            android:text="@string/search_button"
            app:icon="@drawable/search_icon" />

        <Button
            android:id="@+id/rankingsBtn"
            style="@style/OrangeButton"
            android:text="Rankings"
            app:icon="@drawable/baseline_emoji_events_black_24" />

        <Button
            android:id="@+id/learnBtn"
            style="@style/GreenButton"
            android:text="@string/howTo_button"
            app:icon="@drawable/outline_info_black_24" />


    </LinearLayout>


    <Button
        android:id="@+id/bottomSheetExpandButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:background="@android:color/transparent"
        app:icon="@drawable/baseline_keyboard_double_arrow_up_black_36"
        app:iconGravity="textStart"
        app:iconPadding="0dp"
        app:iconTint="@color/orange" />


    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">

        <FrameLayout
            android:id="@+id/standard_bottom_sheet"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_behavior="com.google.android.material.bottomsheet.BottomSheetBehavior">

            <!-- Bottom sheet contents. -->


        </FrameLayout>

    </androidx.coordinatorlayout.widget.CoordinatorLayout>


</RelativeLayout>